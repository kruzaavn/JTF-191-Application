version: '3.8'

services:
  nginx:
    image: linuxserver/letsencrypt
    ports:
      - "443:443"
      - "80:80"
    environment:
      - URL=jtf191.com
      - VALIDATION=http
    volumes:
      - ./nginx/nginx.prod.conf:/config/nginx/site-confs/default
    depends_on:
      - api-server
      - ui
